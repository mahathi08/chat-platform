const ChannelSidebar = () => {
    return (
        <aside className="w-72 border-r border-zinc-800 bg-zinc-900 flex items-center justify-center text-zinc-400">
            Channel Sidebar
        </aside>
    );
};

export default ChannelSidebar;